/**\r\n * @file ble_ftms.h\r\n * @brief BLE Fitness Machine Service (FTMS) - GATT Server\r\n *\r\n * Exposes a Fitness Machine Service (UUID 0x1826) with a Treadmill Data\r\n * characteristic (UUID 0x2AD1) that sends speed data via BLE notifications.\r\n * Compatible with AITrainer and any FTMS-compliant fitness app.\r\n */\r\n\r\n#ifndef BLE_FTMS_H\r\n#define BLE_FTMS_H\r\n\r\n#include <stdbool.h>\r\n\r\n#ifdef __cplusplus\r\nextern \"C\" {\r\n#endif\r\n\r\n/**\r\n * @brief Initialize the FTMS GATT service.\r\n *\r\n * Must be called BEFORE nimble_port_freertos_init() so that the GATT\r\n * service table is registered before the NimBLE host task starts.\r\n */\r\nvoid ble_ftms_init(void);\r\n\r\n/**\r\n * @brief Start BLE advertising with the FTMS service UUID.\r\n *\r\n * Should be called from ble_client_on_sync() after NimBLE has synced.\r\n * Advertising will include the device name \"Treadmill\" and the\r\n * Fitness Machine Service UUID (0x1826).\r\n */\r\nvoid ble_ftms_start_advertising(void);\r\n\r\n/**\r\n * @brief Send a speed update via BLE notification.\r\n *\r\n * Constructs a 4-byte Treadmill Data packet per the FTMS spec:\r\n *   Byte 0-1: Flags (0x0000 — speed present)\r\n *   Byte 2-3: Instantaneous Speed (uint16, resolution 0.01 km/h)\r\n *\r\n * @param speed_kmh Current treadmill speed in km/h\r\n */\r\nvoid ble_ftms_update_speed(float speed_kmh);\r\n\r\n/**\r\n * @brief Check if a client is subscribed to treadmill data notifications.\r\n * @return true if at least one client is subscribed\r\n */\r\nbool ble_ftms_is_subscribed(void);\r\n\r\n#ifdef __cplusplus\r\n}\r\n#endif\r\n\r\n#endif // BLE_FTMS_H\r\n
